# 复制为 deploy/dev/.env（本机开发中间件，不含 MySQL/Redis）

# LiveKit ICE 宣告 IP（换网后 ./deploy/dev/ip.sh -w 自动更新）
LIVEKIT_NODE_IP=127.0.0.1

# RustFS（录制开发，映射到宿主 17886 供 Egress 连接）
RUSTFS_PORT=17886
RUSTFS_CONSOLE_PORT=17887
RUSTFS_ACCESS_KEY=rustfsadmin
RUSTFS_SECRET_KEY=rustfsadmin
RUSTFS_BUCKET=recordings
